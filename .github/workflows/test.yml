name: Deploy

on: [push]

jobs:
  deploy:
    name: Deploy my app

    runs-on: ubuntu-latest
    env:
      TEST_CHAR : "HELLo"
      # CURRENT_DATE : "$(date +%Y%m%d)"".md"

    steps:
      - name : Set Timezone
        uses : 
      - uses: actions/checkout@v3
      - name: Deploy my app
        run: |
          echo "Hello" | cat README.md | bash test.sh